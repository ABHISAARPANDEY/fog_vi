# FogVision AI — backend dependencies
# Requires Python 3.11 or 3.12 (PyTorch/Ultralytics have no 3.14 wheels yet).

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

numpy==1.26.4
opencv-python==4.10.0.84

# Ultralytics pulls in a compatible torch/torchvision build for your platform.
ultralytics==8.3.55
# Required by the ByteTrack tracker (otherwise Ultralytics auto-installs it at runtime).
lap>=0.5.13
